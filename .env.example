DB_HOST=localhost
DB_USER=root
DB_PASS=
# Nama database
DB_MAIN=belanja_modal
DB_INV=db_inventaris

# Debug mode: true = tampilkan error di layar (dev), false = sembunyikan (production, tetap ke log)
APP_DEBUG=false
